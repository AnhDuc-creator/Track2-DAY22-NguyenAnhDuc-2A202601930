# ── LangChain core (pin < 1.0 vi ragas 0.4 chua ho tro langchain 1.x) ─────
langchain>=0.3.0,<1.0
langchain-core>=0.3.0,<1.0
langchain-community>=0.3.0,<0.4
langchain-text-splitters>=0.3.0,<1.0

# ── LLM Providers ─────────────────────────────────────────────────────────
langchain-openai>=0.3.0,<1.0
langchain-anthropic>=0.3.0,<1.0
langchain-google-genai>=2.0.0,<3.0
langchain-ollama>=0.2.0,<1.0

# ── LangSmith ─────────────────────────────────────────────────────────────
langsmith>=0.3.45,<1.0

# ── Vector store ──────────────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────────────
ragas>=0.4.0,<0.5.0
datasets>=2.0.0
numpy>=1.25.0,<3.0

# ── Guardrails AI (0.11 yeu cau langchain-core>=1.0 -> xung dot voi ragas) ─
guardrails-ai>=0.6.0,<0.7.0
# litellm >=1.93 doi openai>=2.20 va chi co sdist -> phai bien dich Rust tren Windows
litellm==1.80.0

# ── Embedding local (khong ton quota API) ────────────────────────────────
fastembed>=0.3.0,<0.8.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=1.0.0,<2.0
tiktoken>=0.5.0